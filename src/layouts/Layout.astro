---
interface Props {
  title: string;
  description?: string;
  image?: string;
}

const GTM_ID = 'GTM-5CKC7W8X';

const { title, description = "Digital Experience Leader & UX/UI Designer in Scottsdale, AZ. 18+ years transforming business objectives into exceptional user experiences.", image = "/images/og-default.jpg" } = Astro.props;
const currentPath = Astro.url.pathname;
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
const canonicalURL = new URL(currentPath, Astro.site || 'https://savelledesign.com');
const ogImage = new URL(image, Astro.site || 'https://savelledesign.com');
---

<!doctype html>
<html lang="en">
  <head>
    <!-- Google Tag Manager -->
    <script define:vars={{ gtmId: GTM_ID }}>
      window.dataLayer = window.dataLayer || [];
      // Normalized gtag shim: turn gtag('event', name, params) into a clean
      // dataLayer push ({event: name, ...params}) so GTM Custom Event triggers
      // fire. gtm.js/config/set/consent commands still push raw arguments.
      window.gtag = function gtag() {
        const a = arguments;
        if (a[0] === 'event' && typeof a[1] === 'string') {
          window.dataLayer.push(Object.assign({ event: a[1] }, a[2] || {}));
        } else {
          window.dataLayer.push(a);
        }
      };
      (function (w, d, s, l, i) {
        w[l] = w[l] || [];
        w[l].push({ 'gtm.start': new Date().getTime(), event: 'gtm.js' });
        const f = d.getElementsByTagName(s)[0];
        const j = d.createElement(s);
        const dl = l !== 'dataLayer' ? '&l=' + l : '';
        j.async = true;
        j.src = 'https://www.googletagmanager.com/gtm.js?id=' + i + dl;
        f.parentNode.insertBefore(j, f);
      })(window, document, 'script', 'dataLayer', gtmId);
    </script>
    <!-- End Google Tag Manager -->
    <meta charset="UTF-8" />
    <meta name="description" content={description} />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="canonical" href={canonicalURL} />
    <link rel="icon" type="image/svg+xml" href={`${base}/favicon.svg`} />

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={ogImage} />
    <meta property="og:site_name" content="Savelle Design" />

    <!-- Twitter -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:url" content={canonicalURL} />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImage} />

    <!-- Additional SEO -->
    <meta name="author" content="Savelle McThias" />
    <meta name="robots" content="index, follow" />

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet" />
    <title>{title}</title>
  </head>
  <body class="bg-white text-zinc-900 min-h-screen">
    <!-- Google Tag Manager (noscript) -->
    <noscript>
      <iframe src={`https://www.googletagmanager.com/ns.html?id=${GTM_ID}`} height="0" width="0" style="display:none;visibility:hidden"></iframe>
    </noscript>
    <!-- End Google Tag Manager (noscript) -->
    <nav class="sticky top-0 z-50 bg-white border-b border-zinc-200">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-16">
          <a href={`${base}/`} class="text-xl font-bold text-zinc-900 hover:text-zinc-700 transition-colors">
            Savelle Design
          </a>

          <!-- Desktop Navigation -->
          <div class="hidden md:flex items-center space-x-8">
            <!-- Work dropdown -->
            <div class="relative group">
              <a
                href={`${base}/works`}
                class={`flex items-center gap-1 hover:text-zinc-600 transition-colors ${currentPath.includes('/works') || currentPath.includes('/gallery') ? 'text-zinc-900 font-semibold' : 'text-zinc-700'}`}
              >
                Work
                <svg class="w-4 h-4 transition-transform duration-150 group-hover:rotate-180" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
                </svg>
              </a>
              <div class="absolute left-0 top-full pt-3 opacity-0 invisible translate-y-1 group-hover:opacity-100 group-hover:visible group-hover:translate-y-0 group-focus-within:opacity-100 group-focus-within:visible group-focus-within:translate-y-0 transition-all duration-150 z-50">
                <div class="bg-white border border-zinc-200 rounded-lg shadow-lg py-2 min-w-[180px]">
                  <a href={`${base}/works`} class={`block px-4 py-2.5 text-sm hover:bg-zinc-50 hover:text-zinc-900 transition-colors ${currentPath.includes('/works') ? 'text-zinc-900 font-semibold' : 'text-zinc-700'}`}>Case Studies</a>
                  <a href={`${base}/gallery`} class={`block px-4 py-2.5 text-sm hover:bg-zinc-50 hover:text-zinc-900 transition-colors ${currentPath.includes('/gallery') ? 'text-zinc-900 font-semibold' : 'text-zinc-700'}`}>Gallery</a>
                </div>
              </div>
            </div>

            <!-- Services dropdown -->
            <div class="relative group">
              <a
                href={`${base}/services`}
                class={`flex items-center gap-1 hover:text-zinc-600 transition-colors ${currentPath.includes('/services') || currentPath.includes('/ux-health-check') || currentPath.includes('/ux-audit') || currentPath.includes('/pricing') ? 'text-zinc-900 font-semibold' : 'text-zinc-700'}`}
              >
                Services
                <svg class="w-4 h-4 transition-transform duration-150 group-hover:rotate-180" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
                </svg>
              </a>
              <div class="absolute left-0 top-full pt-3 opacity-0 invisible translate-y-1 group-hover:opacity-100 group-hover:visible group-hover:translate-y-0 group-focus-within:opacity-100 group-focus-within:visible group-focus-within:translate-y-0 transition-all duration-150 z-50">
                <div class="bg-white border border-zinc-200 rounded-lg shadow-lg py-2 min-w-[220px]">
                  <a href={`${base}/services`} class={`block px-4 py-2.5 text-sm hover:bg-zinc-50 hover:text-zinc-900 transition-colors ${currentPath === `${base}/services` || currentPath === `${base}/services/` ? 'text-zinc-900 font-semibold' : 'text-zinc-700'}`}>Our Services</a>
                  <a href={`${base}/services/landing-page-design`} class={`block pl-7 pr-4 py-2 text-sm hover:bg-zinc-50 hover:text-zinc-900 transition-colors ${currentPath.includes('/services/landing-page-design') ? 'text-zinc-900 font-semibold' : 'text-zinc-500'}`}>Landing Page Design</a>
                  <a href={`${base}/services/website-refresh`} class={`block pl-7 pr-4 py-2 text-sm hover:bg-zinc-50 hover:text-zinc-900 transition-colors ${currentPath.includes('/services/website-refresh') ? 'text-zinc-900 font-semibold' : 'text-zinc-500'}`}>Website Refresh</a>
                  <a href={`${base}/services/brand-identity`} class={`block pl-7 pr-4 py-2 text-sm hover:bg-zinc-50 hover:text-zinc-900 transition-colors ${currentPath.includes('/services/brand-identity') ? 'text-zinc-900 font-semibold' : 'text-zinc-500'}`}>Brand Identity</a>
                  <a href={`${base}/services/email-campaigns`} class={`block pl-7 pr-4 py-2 text-sm hover:bg-zinc-50 hover:text-zinc-900 transition-colors ${currentPath.includes('/services/email-campaigns') ? 'text-zinc-900 font-semibold' : 'text-zinc-500'}`}>Email Campaigns</a>
                  <div class="my-2 border-t border-zinc-100"></div>
                  <a href={`${base}/pricing`} class={`block px-4 py-2.5 text-sm hover:bg-zinc-50 hover:text-zinc-900 transition-colors ${currentPath.includes('/pricing') ? 'text-zinc-900 font-semibold' : 'text-zinc-700'}`}>Pricing</a>
                  <a href={`${base}/ux-health-check`} class={`block px-4 py-2.5 text-sm hover:bg-zinc-50 hover:text-zinc-900 transition-colors ${currentPath.includes('/ux-health-check') ? 'text-zinc-900 font-semibold' : 'text-zinc-700'}`}>UX Health Check</a>
                  <a href={`${base}/ux-audit`} class={`block px-4 py-2.5 text-sm hover:bg-zinc-50 hover:text-zinc-900 transition-colors ${currentPath.includes('/ux-audit') ? 'text-zinc-900 font-semibold' : 'text-zinc-700'}`}>UX Audit</a>
                </div>
              </div>
            </div>

            <!-- About dropdown -->
            <div class="relative group">
              <a
                href={`${base}/about`}
                class={`flex items-center gap-1 hover:text-zinc-600 transition-colors ${currentPath.includes('/about') || currentPath.includes('/blog') ? 'text-zinc-900 font-semibold' : 'text-zinc-700'}`}
              >
                About
                <svg class="w-4 h-4 transition-transform duration-150 group-hover:rotate-180" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
                </svg>
              </a>
              <div class="absolute left-0 top-full pt-3 opacity-0 invisible translate-y-1 group-hover:opacity-100 group-hover:visible group-hover:translate-y-0 group-focus-within:opacity-100 group-focus-within:visible group-focus-within:translate-y-0 transition-all duration-150 z-50">
                <div class="bg-white border border-zinc-200 rounded-lg shadow-lg py-2 min-w-[180px]">
                  <a href={`${base}/about`} class={`block px-4 py-2.5 text-sm hover:bg-zinc-50 hover:text-zinc-900 transition-colors ${currentPath.includes('/about') ? 'text-zinc-900 font-semibold' : 'text-zinc-700'}`}>About Me</a>
                  <a href={`${base}/blog`} class={`block px-4 py-2.5 text-sm hover:bg-zinc-50 hover:text-zinc-900 transition-colors ${currentPath.includes('/blog') ? 'text-zinc-900 font-semibold' : 'text-zinc-700'}`}>Blog</a>
                </div>
              </div>
            </div>

            <a
              href={`${base}/contact`}
              class={`hover:text-zinc-600 transition-colors ${currentPath.includes('/contact') ? 'text-zinc-900 font-semibold' : 'text-zinc-700'}`}
            >
              Contact
            </a>
          </div>

          <!-- Mobile Menu Button -->
          <button id="mobile-menu-button" class="md:hidden p-2 rounded-lg hover:bg-zinc-100 transition-colors" aria-label="Toggle menu">
            <svg id="menu-icon" class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
            </svg>
            <svg id="close-icon" class="w-6 h-6 hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
            </svg>
          </button>
        </div>
      </div>

      <!-- Mobile Navigation Menu -->
      <div id="mobile-menu" class="hidden md:hidden border-t border-zinc-200">
        <div class="px-4 py-4 space-y-3">
          <a
            href={`${base}/`}
            class={`block py-2 hover:text-zinc-600 transition-colors ${currentPath === base || currentPath === `${base}/` ? 'text-zinc-900 font-semibold' : 'text-zinc-700'}`}
          >
            Home
          </a>
          <a
            href={`${base}/works`}
            class={`block py-2 hover:text-zinc-600 transition-colors ${currentPath.includes('/works') ? 'text-zinc-900 font-semibold' : 'text-zinc-700'}`}
          >
            Case Studies
          </a>
          <a
            href={`${base}/gallery`}
            class={`block py-2 hover:text-zinc-600 transition-colors ${currentPath.includes('/gallery') ? 'text-zinc-900 font-semibold' : 'text-zinc-700'}`}
          >
            Gallery
          </a>
          <a
            href={`${base}/services`}
            class={`block py-2 hover:text-zinc-600 transition-colors ${currentPath === `${base}/services` || currentPath === `${base}/services/` ? 'text-zinc-900 font-semibold' : 'text-zinc-700'}`}
          >
            Our Services
          </a>
          <a
            href={`${base}/services/landing-page-design`}
            class={`block py-2 pl-4 text-sm hover:text-zinc-600 transition-colors ${currentPath.includes('/services/landing-page-design') ? 'text-zinc-900 font-semibold' : 'text-zinc-500'}`}
          >
            Landing Page Design
          </a>
          <a
            href={`${base}/services/website-refresh`}
            class={`block py-2 pl-4 text-sm hover:text-zinc-600 transition-colors ${currentPath.includes('/services/website-refresh') ? 'text-zinc-900 font-semibold' : 'text-zinc-500'}`}
          >
            Website Refresh
          </a>
          <a
            href={`${base}/services/brand-identity`}
            class={`block py-2 pl-4 text-sm hover:text-zinc-600 transition-colors ${currentPath.includes('/services/brand-identity') ? 'text-zinc-900 font-semibold' : 'text-zinc-500'}`}
          >
            Brand Identity
          </a>
          <a
            href={`${base}/services/email-campaigns`}
            class={`block py-2 pl-4 text-sm hover:text-zinc-600 transition-colors ${currentPath.includes('/services/email-campaigns') ? 'text-zinc-900 font-semibold' : 'text-zinc-500'}`}
          >
            Email Campaigns
          </a>
          <a
            href={`${base}/pricing`}
            class={`block py-2 hover:text-zinc-600 transition-colors ${currentPath.includes('/pricing') ? 'text-zinc-900 font-semibold' : 'text-zinc-700'}`}
          >
            Pricing
          </a>
          <a
            href={`${base}/about`}
            class={`block py-2 hover:text-zinc-600 transition-colors ${currentPath.includes('/about') ? 'text-zinc-900 font-semibold' : 'text-zinc-700'}`}
          >
            About Me
          </a>
          <a
            href={`${base}/blog`}
            class={`block py-2 hover:text-zinc-600 transition-colors ${currentPath.includes('/blog') ? 'text-zinc-900 font-semibold' : 'text-zinc-700'}`}
          >
            Blog
          </a>
          <a
            href={`${base}/ux-health-check`}
            class={`block py-2 hover:text-zinc-600 transition-colors ${currentPath.includes('/ux-health-check') ? 'text-zinc-900 font-semibold' : 'text-zinc-700'}`}
          >
            UX Health Check
          </a>
          <a
            href={`${base}/ux-audit`}
            class={`block py-2 hover:text-zinc-600 transition-colors ${currentPath.includes('/ux-audit') ? 'text-zinc-900 font-semibold' : 'text-zinc-700'}`}
          >
            UX Audit
          </a>
          <a
            href={`${base}/contact`}
            class={`block py-2 hover:text-zinc-600 transition-colors ${currentPath.includes('/contact') ? 'text-zinc-900 font-semibold' : 'text-zinc-700'}`}
          >
            Contact
          </a>
        </div>
      </div>
    </nav>

    <main>
      <slot />
    </main>

    <footer class="bg-zinc-900">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16">
        <div class="grid grid-cols-2 md:grid-cols-5 gap-8">
          <div class="col-span-2">
            <a href={`${base}/`} class="text-xl font-bold text-white hover:text-zinc-300 transition-colors">
              Savelle Design
            </a>
            <p class="mt-4 text-sm text-zinc-400 max-w-xs">
              UX/UI design and web development for growing businesses, based in Scottsdale, AZ.
            </p>
            <a
              href="https://www.linkedin.com/in/savelle/"
              target="_blank"
              rel="noopener noreferrer"
              aria-label="Savelle McThias on LinkedIn"
              class="mt-6 inline-flex items-center justify-center w-9 h-9 rounded-lg bg-zinc-800 text-zinc-400 hover:bg-zinc-700 hover:text-white transition-colors"
            >
              <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                <path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/>
              </svg>
            </a>
          </div>

          <div>
            <h2 class="text-xs font-semibold text-zinc-200 uppercase tracking-wider mb-4">Explore</h2>
            <ul class="space-y-3 text-sm">
              <li><a href={`${base}/`} class="text-zinc-400 hover:text-white transition-colors">Home</a></li>
              <li><a href={`${base}/about`} class="text-zinc-400 hover:text-white transition-colors">About</a></li>
              <li><a href={`${base}/works`} class="text-zinc-400 hover:text-white transition-colors">Works</a></li>
              <li><a href={`${base}/gallery`} class="text-zinc-400 hover:text-white transition-colors">Gallery</a></li>
              <li><a href={`${base}/blog`} class="text-zinc-400 hover:text-white transition-colors">Blog</a></li>
            </ul>
          </div>

          <div>
            <h2 class="text-xs font-semibold text-zinc-200 uppercase tracking-wider mb-4">Services</h2>
            <ul class="space-y-3 text-sm">
              <li><a href={`${base}/services`} class="text-zinc-400 hover:text-white transition-colors">All Services</a></li>
              <li><a href={`${base}/services/landing-page-design`} class="text-zinc-400 hover:text-white transition-colors">Landing Page Design</a></li>
              <li><a href={`${base}/services/website-refresh`} class="text-zinc-400 hover:text-white transition-colors">Website Refresh</a></li>
              <li><a href={`${base}/services/brand-identity`} class="text-zinc-400 hover:text-white transition-colors">Brand Identity</a></li>
              <li><a href={`${base}/services/email-campaigns`} class="text-zinc-400 hover:text-white transition-colors">Email Campaigns</a></li>
              <li><a href={`${base}/pricing`} class="text-zinc-400 hover:text-white transition-colors">Pricing</a></li>
            </ul>
          </div>

          <div>
            <h2 class="text-xs font-semibold text-zinc-200 uppercase tracking-wider mb-4">Get Started</h2>
            <ul class="space-y-3 text-sm">
              <li><a href={`${base}/ux-health-check`} class="text-zinc-400 hover:text-white transition-colors">UX Health Check</a></li>
              <li><a href={`${base}/ux-audit`} class="text-zinc-400 hover:text-white transition-colors">UX Audit</a></li>
              <li><a href={`${base}/contact`} class="text-zinc-400 hover:text-white transition-colors">Contact</a></li>
              <li><a href="mailto:savellem@gmail.com" class="text-zinc-400 hover:text-white transition-colors">savellem@gmail.com</a></li>
            </ul>
          </div>
        </div>

        <div class="mt-12 pt-8 border-t border-zinc-800 flex flex-col sm:flex-row justify-between items-center gap-4 text-sm text-zinc-500">
          <p>&copy; {new Date().getFullYear()} Savelle Design. All rights reserved.</p>
          <div class="flex items-center gap-6">
            <a href={`${base}/privacy`} class="hover:text-zinc-200 transition-colors">Privacy Policy</a>
            <a href={`${base}/terms`} class="hover:text-zinc-200 transition-colors">Terms of Service</a>
          </div>
        </div>
      </div>
    </footer>

    <!-- Structured Data -->
    <script type="application/ld+json">
      {
        "@context": "https://schema.org",
        "@type": "ProfessionalService",
        "name": "Savelle Design",
        "description": "Digital Experience Leader & UX/UI Designer specializing in landing pages, website refresh, brand identity, and email campaigns for growing businesses.",
        "url": "https://savelledesign.com",
        "logo": "https://savelledesign.com/favicon.svg",
        "image": "https://savelledesign.com/images/og-default.jpg",
        "founder": {
          "@type": "Person",
          "name": "Savelle McThias",
          "jobTitle": "UX/UI Designer & Developer",
          "email": "savellem@gmail.com",
          "url": "https://savelledesign.com/about"
        },
        "address": {
          "@type": "PostalAddress",
          "addressLocality": "Scottsdale",
          "addressRegion": "AZ",
          "addressCountry": "US"
        },
        "areaServed": "Worldwide",
        "priceRange": "$$",
        "sameAs": [
          "https://www.linkedin.com/in/savelle/"
        ]
      }
    </script>
  </body>
</html>

<style is:global>
  * {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
  }

  html {
    font-family: 'Plus Jakarta Sans', system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
  }
</style>

<script>
  // Mobile menu toggle functionality
  const mobileMenuButton = document.getElementById('mobile-menu-button');
  const mobileMenu = document.getElementById('mobile-menu');
  const menuIcon = document.getElementById('menu-icon');
  const closeIcon = document.getElementById('close-icon');

  mobileMenuButton?.addEventListener('click', () => {
    const isHidden = mobileMenu?.classList.contains('hidden');

    if (isHidden) {
      mobileMenu?.classList.remove('hidden');
      menuIcon?.classList.add('hidden');
      closeIcon?.classList.remove('hidden');
    } else {
      mobileMenu?.classList.add('hidden');
      menuIcon?.classList.remove('hidden');
      closeIcon?.classList.add('hidden');
    }
  });

  // Close mobile menu when clicking on a link
  const mobileMenuLinks = mobileMenu?.querySelectorAll('a');
  mobileMenuLinks?.forEach((link) => {
    link.addEventListener('click', () => {
      mobileMenu?.classList.add('hidden');
      menuIcon?.classList.remove('hidden');
      closeIcon?.classList.add('hidden');
    });
  });
</script>
